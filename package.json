{
  "name": "neko-control-room",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "npm run build:standalone",
    "build:standalone": "node scripts/build-standalone.mjs",
    "start": "node admin-api/src/server.mjs",
    "admin:start": "node admin-api/src/server.mjs",
    "test": "npm run build:standalone && node --test tests/admin-api.test.mjs",
    "admin:test": "node --test tests/admin-api.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.8"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
