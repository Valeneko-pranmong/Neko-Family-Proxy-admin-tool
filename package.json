{
  "name": "neko-control-room",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm run build:standalone",
    "build:standalone": "node scripts/build-standalone.mjs",
    "test": "npm run build:standalone && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.8"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
